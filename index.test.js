// describe("GET /users", () => {
//     test("GET /users", async () => {
//       const response = await request(app).get("/users")
//     })
//   })

//   describe("POST /users", () => {
//     test("POST /users updates the users array", async () => {
//       const newUser = {name: "Bilal"}
//       const response = await request(app)
//         .post("/users")
//         .send(newUser);

//       const updatedUsers = response.body
//       expect(updatedUsers).toContain(newUser.name)
//     })
//   } )
